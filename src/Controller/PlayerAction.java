package Controller;

/**
 * PlayerAction is an enum containing the different kinds of actions a yahtzee player may
 * perform when picking a scoreboard cell.
 * @author Isak
 */
public enum PlayerAction {
    SCORE,
    CROSS
}
